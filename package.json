{
  "scripts": {
    "build": "mkdir -p build && resumed render -t jsonresume-theme-actual -o build/index.html",
    "validate": "resumed validate"
  },
  "dependencies": {
    "jsonresume-theme-even": "latest",
    "resumed": "latest"
  }
}
